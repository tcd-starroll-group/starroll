apiVersion: v1
kind: Service
metadata:
  name: grafana-service
  namespace: starroll
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  selector:
    app: grafana
  ports:
    - name: grafana
      protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
